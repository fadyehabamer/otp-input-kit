## What does this PR do?

<!-- A short description of the change and why it's needed. -->

Closes #

## Checklist

- [ ] `npm test` passes, and new behaviour has tests
- [ ] `npm run build` passes and the regenerated `dist/` is committed
- [ ] Types (`types/*.d.ts`), README and every affected wrapper are updated
- [ ] `CHANGELOG.md` has an entry under `## [Unreleased]`
- [ ] Checked in LTR and RTL (and on a real device, for mobile / SMS changes)

## Screenshots (if visual)

<!-- Before / after, ideally in LTR and RTL. -->
